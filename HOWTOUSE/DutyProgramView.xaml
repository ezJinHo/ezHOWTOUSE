<UserControl x:Class="HOWTOUSE.DutyProgramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="TabButton" TargetType="Button">
            <Setter Property="MinWidth" Value="118" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#64748B" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18,0" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="11"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="당직 프로그램" FontSize="28" FontWeight="Bold" Foreground="#0F172A" />
            <TextBlock Text="전산실 내부에서 당직 매뉴얼, 일정, 스케줄 교환을 관리합니다."
                       Margin="0,6,0,0"
                       FontSize="14"
                       Foreground="#64748B" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="#F7F9FC"
                BorderBrush="#E2E8F0"
                BorderThickness="1"
                CornerRadius="14"
                Padding="6"
                HorizontalAlignment="Left"
                Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="ManualTabButton" Content="당직 매뉴얼" Tag="Manual" Style="{StaticResource TabButton}" Click="DutyTabButton_Click" />
                <Button x:Name="ScheduleTabButton" Content="당직일정" Tag="Schedule" Style="{StaticResource TabButton}" Click="DutyTabButton_Click" />
                <Button x:Name="SwapTabButton" Content="스케줄 교환" Tag="Swap" Style="{StaticResource TabButton}" Click="DutyTabButton_Click" />
            </StackPanel>
        </Border>

        <ContentControl x:Name="DutyContentControl" Grid.Row="2" />
    </Grid>
</UserControl>
