<UserControl x:Class="HOWTOUSE.SurveyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="Panel" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D8E0EA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CBD5E1" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="사용자 만족도 조사" FontSize="28" FontWeight="Bold" Foreground="#0F172A" />
            <TextBlock Text="사용자가 프로그램과 매뉴얼에 대한 만족도를 제출합니다." Margin="0,6,0,0" FontSize="14" Foreground="#64748B" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Style="{StaticResource Panel}">
                <StackPanel>
                    <TextBlock Text="사용자 만족도 조사" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock Text="프로그램 사용 경험과 매뉴얼 품질을 조사합니다." Margin="0,5,0,14" Foreground="#64748B" TextWrapping="Wrap" />
                    <TextBlock Text="만족도 점수" FontWeight="SemiBold" />
                    <Slider x:Name="SatisfactionSlider" Minimum="1" Maximum="5" Value="4" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,6,0,12" />
                    <TextBlock Text="의견" FontWeight="SemiBold" />
                    <TextBox x:Name="SurveyCommentInput" Margin="0,5,0,12" MinHeight="150" AcceptsReturn="True" TextWrapping="Wrap" />
                    <Button Content="만족도 제출" Style="{StaticResource PrimaryButton}" Click="SaveSurveyButton_Click" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Panel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="조사 현황" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock x:Name="SurveyAverageText" Grid.Row="1" Margin="0,8,0,12" Foreground="#2563EB" FontWeight="SemiBold" />
                    <ListBox x:Name="SurveyListBox" Grid.Row="2" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
