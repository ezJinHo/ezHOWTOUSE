<UserControl x:Class="HOWTOUSE.SuggestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="Panel" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D8E0EA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CBD5E1" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="제안사항" FontSize="28" FontWeight="Bold" Foreground="#0F172A" />
            <TextBlock Text="사용자가 추가되었으면 하는 매뉴얼을 제안합니다." Margin="0,6,0,0" FontSize="14" Foreground="#64748B" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Style="{StaticResource Panel}">
                <StackPanel>
                    <TextBlock Text="매뉴얼 제안" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock Text="추가되었으면 하는 매뉴얼을 사용자들이 제안할 수 있습니다." Margin="0,5,0,14" Foreground="#64748B" TextWrapping="Wrap" />
                    <TextBlock Text="제목" FontWeight="SemiBold" />
                    <TextBox x:Name="SuggestionTitleInput" Margin="0,5,0,12" />
                    <TextBlock Text="필요한 이유" FontWeight="SemiBold" />
                    <TextBox x:Name="SuggestionBodyInput" Margin="0,5,0,12" MinHeight="160" AcceptsReturn="True" TextWrapping="Wrap" />
                    <Button Content="제안 등록" Style="{StaticResource PrimaryButton}" Click="SaveSuggestionButton_Click" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Panel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="등록된 제안" FontSize="18" FontWeight="SemiBold" />
                    <ListBox x:Name="SuggestionListBox" Grid.Row="1" Margin="0,14,0,0" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
